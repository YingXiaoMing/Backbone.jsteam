${toolbarTemplate(buttons)}
<div class="view clearfix">
	<form class="form-horizontal ui-sortable" role="form" id="${name}" name="${name}" action="${action}" enctype="${enctype}" method="${method}">
		<div class="row base row-form clearfix">
			<div class="col-md-12 column sortable"></div>
		</div>
	</form>
</div>
				
